<!-- Divergence investigation report skeleton: replace the `<...>` placeholders and the "..." cells in the table with real data. Open one `## D-{n}` section per divergence. -->

# Divergence Investigation Report — Round <N>

- **Date:** YYYY-MM-DD
- **Divergences detected:** <N>

## D-1 — <one-line summary of the divergence>

- **Kind:** <Non-shrinking chain / Oscillation>
- **Chain:** R1 M-2 → R2 M-1 → R3 mi-3

### History

| Round | ID | Location | Finding | Fix |
|-------|----|----------|---------|-----|
| Round 1 | M-2 | `file.cpp:42` | ... | ... |
| Round 2 | M-1 | `file.cpp:48` | ... | ... |
| Round 3 | mi-3 | `file.cpp:51` | ... | (not fixed yet) |

### Source

<The root cause that keeps findings coming however many fixes are stacked, and its evidence (code locations, commits, review document passages)>

### Fix proposals

1. **<proposal name> (recommended)** — <content / why it stops the chain / impact range>
2. **<proposal name>** — <content / why it stops the chain / impact range>

---
